Installation process:
    node -v
    node --version
    npm -v
    npm --version
    make sure you are in the vite-project directory (cd Frontend/vite-project)
    npm install
After Installation:
to run the program navigate to the vite-project then execute in the termianl npm run dev
Backend:
Ensure this matches your Flask app's port (by default http://localhost:5001)